## 2025-11-16 Renderer modularization
- `apps/desktop-electron/src/renderer/app.ts` now consumes node-specific renderer modules (`nodes/load.ts`, `nodes/trim.ts`) via `createNodeRenderers`, adds `data-type-id`/`node-type-*` classes, and routes node deletion/resize events through the registry.
- Load node preview/media logic fully moved to `nodes/load.ts`, keeping per-node cleanup and preview measurement encapsulated.
- `ui-template.ts` bundles renderer modules recursively (subdirectories) and now scopes load-node styles via `.node-type-*` selectors; delete button styles override the global button rule to keep the tiny red circle stable.
- `ui-template.test.ts` gained coverage for the new node-type metadata and the Vitest suite plus `pnpm --filter desktop-electron build` remain green (100% coverage).